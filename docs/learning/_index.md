# Learning Index

Running table of contents for `docs/learning/`. Read this before writing a new doc so new
entries link to (and don't repeat) what's already covered.

| Doc | Summary | Tags |
| --- | --- | --- |
| [django-project-vs-app.md](django-project-vs-app.md) | What a Django "project" (the whole deployable container) vs. an "app" (a self-contained feature module) is, and what `manage.py startapp` actually creates. | django, web-fundamentals, scaffolding |
| [what-a-web-app-needs-to-run.md](what-a-web-app-needs-to-run.md) | The five things any web app needs (entry point, config, routing, handler code, and an actual running server process) generalized beyond Django, then mapped onto `settings.py`, `urls.py`, `wsgi.py`, `asgi.py`, and `manage.py runserver`/`gunicorn` — what each does, why it exists, and how to confirm the process is really running. | django, web-fundamentals, deployment |
| [database-migrations-and-dev-prod-parity.md](database-migrations-and-dev-prod-parity.md) | What a Django migration is, how `django_migrations` tracks what's applied, why the ORM lets the same code target SQLite (dev) and Postgres (prod), and the concrete parity gap in this project's deployment plan (first Postgres migration run happens at deploy time). | django, web-fundamentals, deployment, database |
| [python-dependency-management-pip-uv-venv.md](python-dependency-management-pip-uv-venv.md) | How virtual environments (isolation), pip (installation), and uv (installation + venv management + a reproducible lockfile) each solve a different piece of "getting the right packages installed"; what TOML is and how `pyproject.toml` (hand-edited intent) differs from `uv.lock` (generated, exact); why `uv.lock` lists two Django versions with `resolution-markers`. | python, tooling, web-fundamentals |
| [from-dev-scaffold-to-production-ready.md](from-dev-scaffold-to-production-ready.md) | Map of Phase 0 of the deployment plan: why every `startproject` default is wrong in production, how flipping `DEBUG` silently changes four subsystems at once, why production adds a supervisor that judges the app by a health-check endpoint (and rolls back on a 404 or 400), plus `.python-version`, `check --deploy`, and two real bugs in the plan as written. | deployment, django, production, web-fundamentals |
| [config-in-dev-vs-prod.md](config-in-dev-vs-prod.md) | How one codebase behaves differently per environment: what an environment variable is and who sets it (shell, `.env`, `railway variable set`, Railway reference variables), the "everything in the environment is a string" wart behind `bool("False") is True` and `"".split(",") == [""]`, and what `SECRET_KEY`, `DEBUG`, `ALLOWED_HOSTS`, and `DATABASE_URL` each actually control. | deployment, configuration, django, security |
| [static-files-collectstatic-and-whitenoise.md](static-files-collectstatic-and-whitenoise.md) | Static (byte-for-byte) vs. dynamic (computed) responses; why Django scatters assets across installed apps and `collectstatic` rakes them into one `STATIC_ROOT`; why the dev-only auto-serving switches off at `DEBUG=False`; what WhiteNoise does instead of nginx/CDN; and the manifest-vs-compressed `STORAGES` trade-off. | deployment, django, static-files, web-fundamentals |
| [ci-cd-and-deploy-triggers.md](ci-cd-and-deploy-triggers.md) | What CI and CD each actually answer; what a git remote, a pull request, and an ephemeral runner are; how a repository secret lets a blank build machine deploy to Railway, and why that makes the merge button the real access control; the two workflow subtleties that bite (a path filter on `pull_request` deadlocks required checks; deploys must serialise because `migrate` runs at container start); and why this project chose GitHub Actions over Railway's native GitHub integration. | deployment, ci-cd, github, automation, security |
| [automated-testing-types-and-django-mechanics.md](automated-testing-types-and-django-mechanics.md) | Why a test suite is "the accumulated memory of every manual check you already did"; the unit / integration / smoke / end-to-end scope ladder and why the pyramid shape follows from failure diagnosability rather than speed; why "regression" is an orthogonal axis (why a test exists) rather than a fifth scope, and why regression tests are proven where up-front tests are speculative; what tests provably cannot tell you (a leaking view still returns 200; a green SQLite suite says nothing about Postgres case-sensitivity); and the Django-specific mechanics — discovery by `test*.py` naming, the throwaway migrated test database, per-test transaction rollback, and the in-process test client — mapped onto the four test files planned for the households app. | testing, django, ci-cd, quality, web-fundamentals |
| [csrf-and-https-behind-a-proxy.md](csrf-and-https-behind-a-proxy.md) | The CSRF attack (browsers auto-attaching cookies to cross-site requests) and the token that defeats it; why TLS termination at a platform edge makes Django believe it's serving plain HTTP; and how `SECURE_PROXY_SSL_HEADER` and `CSRF_TRUSTED_ORIGINS` fix two different failures that print the same "CSRF verification failed" message. | security, deployment, django, web-fundamentals |
| [orm-models-and-sql-ddl.md](orm-models-and-sql-ddl.md) | What a Django model class field-by-field becomes as generated DDL (`households/models.py` vs. `households/migrations/0001_initial.py`); why the model class *is* the schema rather than a description kept in sync with one; and `ForeignKey` vs. `OneToOneField` — the same FK-plus-constraint shape, except `OneToOneField` adds `UNIQUE`, which is why `Membership.user` enforces one-household-per-user at the database layer instead of in view code. | django, orm, database, web-fundamentals |
| [sessions-and-login-persistence.md](sessions-and-login-persistence.md) | Why HTTP has no memory and what a session cookie actually does about it (opaque key in the cookie, real data in `django_session`); what `login()`/`logout()` write into `request.session` and why login rotates the session key; and this project's own non-auth use of the same mechanism — stashing an invite token across the anonymous-join-to-signup round trip, and why `signup()` reads it with `.pop()` instead of a plain lookup. | django, sessions, auth, web-fundamentals |
| [django-templates-and-css.md](django-templates-and-css.md) | How `{% extends %}` / `{% block %}` let every page in `households/templates/` share one `base.html` skeleton; how `{% static %}` resolves a stylesheet path via Django's staticfiles finders in dev vs. via `collectstatic` output in prod; and why `base.html` loads a vendored classless stylesheet (Pico) before a project override file, styling bare HTML elements with no CSS classes in any template. | django, templates, static-files, css, web-fundamentals |
| [branches-worktrees-and-parallel-work.md](branches-worktrees-and-parallel-work.md) | What a git branch physically is (a movable pointer, not a copy) and why one folder therefore holds exactly one branch's files at a time; what `git worktree` adds (a second working tree over one shared history) and what it deliberately does not carry across (`.venv`, the gitignored `db.sqlite3` with its 20k imported products); why two branches each adding a `registry/migrations/0002_*` file breaks Django rather than git; and why `git branch --merged` reports squash-merged branches as unmerged. | git, workflow, tooling, ci-cd |
| [unguessable-invite-tokens.md](unguessable-invite-tokens.md) | Why `Household.invite_token` uses `secrets.token_urlsafe(32)` — the cryptographic-unpredictability property a bearer token actually needs, why `random`'s Mersenne Twister fails it, and the real (narrower) reasons `uuid4` is avoided even though it also draws from `os.urandom`; and why overwriting the token is a complete revocation with no separate expiry or denylist needed. | security, django, web-fundamentals |
